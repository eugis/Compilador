    int x, y, z;
    y = 10;
    x = 6 + y;
    write(y);
    x = ++y;
    write(y);
    write(x);
    z = 3;
  while (2>3);
  for (x = 1; x < 10; x++) {
    if (x > 4 && x < 6) {continue;}

    write(x);
  }
