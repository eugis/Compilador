int base, exponente, ans, i;
base = read("Ingrese la base:");
exponente = read("Ingrese el exponente:");

if(exponente == 0){
    if(base !=0){
        write("La potencia es 1");
    }
    else
        write("Error: es una indeterminación");
}else{
    if(exponente < 0){
        exponente = - exponente;
    }
    i = 1;
    ans = base;
    while(i < exponente)
        ans = ans * base;
    write("La potencia es");
    write(ans);
}