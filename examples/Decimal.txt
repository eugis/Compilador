int num, ans, i;

num = read("Ingrese el número en binario que desee pasar:");
i = 1;
ans = 0;
while (num >= 1){
    ans = ans + (num%10)*i;
    num = num/10;
    i=i*2;
}
write("El número en decimal es: ");
write(ans);